# Python + Agent 前端开发：12 周从 0 到 1 学习计划

这是一条给前端开发者的实战路线：用 12 周、每周 5 天、共 60 个学习日，把 Python 基础推进到可以独立设计、开发、测试和部署一个 Agent 前端产品。

主线顺序固定为：

**Python 基础 -> API 服务 -> LLM 调用 -> Tool Calling -> RAG -> Agent 工作流 -> MCP -> 流式交互 -> 生产化**

这里的“Agent 前端开发”不是只做一个聊天框，而是同时掌握：

- 用 Python 编写可靠的 API、数据处理和 Agent 服务。
- 理解模型调用、工具调用、状态、记忆、RAG 和工作流。
- 用 React/TypeScript（沿用已有前端优势）做流式聊天、工具过程、引用来源和错误状态。
- 把项目做成可运行、可测试、可观测、可部署的产品。

## 1. 目标与学习边界

### 12 周后的能力目标

你应当能够：

1. 使用 Python 3.12+、虚拟环境、类型标注、测试和异步编程完成中小型服务。
2. 使用 FastAPI + Pydantic 设计 JSON API、SSE 流式接口和基础鉴权。
3. 调用任意 OpenAI-compatible 模型服务，处理结构化输出、流式响应、重试和成本记录。
4. 实现符合 Tool Calling 约定的工具 schema、工具注册、执行循环、重试和人工确认。
5. 实现一个可追溯的 RAG 流程：导入、切分、向量化、检索、引用和评测。
6. 使用工作流编排多个步骤，并理解状态、暂停、恢复和失败转移。
7. 理解 MCP 的 Host/Client/Server 模型，能够编写 MCP Server 并将其接入 Agent。
8. 将 Agent 接入前端，并处理流式 token、工具过程、取消、断线重连、空态和错误态。
9. 用 Docker、日志、测试和 CI 把项目交付到可演示环境。

### 不在本计划内的内容

本计划不追求一次性覆盖所有 Python 生态、深度学习训练或复杂分布式系统。先建立“能交付 Agent 产品”的闭环，再按项目需要深入 NumPy、PyTorch、复杂多智能体或大规模向量数据库。

### 时间安排

- 每天：建议 2～2.5 小时（30～40 分钟学习，80～100 分钟编码，10～20 分钟记录）。
- 每周：5 天，约 10～12.5 小时。周末只做可选复盘，不计入 60 天。
- 每天结束前必须留下一个可验证产出：代码、测试、接口、截图、短文档或评测结果。
- 每周最后一天安排复盘和小交付，不能只看视频。

## 2. 先准备好环境

### 必备工具

- Python 3.12 或更高版本
- Git、VS Code（或熟悉的编辑器）
- uv（推荐）或 venv + pip
- Node.js 20+ 和 pnpm（用于 Agent 前端）
- Docker Desktop（第 10 周开始使用）
- 一个 OpenAI-compatible 模型服务的 API Key；没有 Key 时，第 5～8 周可先使用 mock 响应

### 初始化 Python 项目

```bash
mkdir agent-python-lab
cd agent-python-lab
uv init
uv venv
source .venv/bin/activate       # Windows: .venv\Scripts\activate
uv add fastapi "uvicorn[standard]" pydantic-settings httpx pytest pytest-asyncio ruff mypy
uv run python --version
git init
```

建议建立如下目录，并在第一天提交：

```text
agent-python-lab/
├── app/                  # 可复用的应用代码
├── tests/                # 单元测试与集成测试
├── scripts/              # 一次性脚本与数据处理脚本
├── docs/                 # ADR、接口和评测记录
├── .env.example
├── pyproject.toml
└── README.md
```

不要把真实密钥提交到 Git。统一使用 `.env`，并提交 `.env.example`。

## 3. 每日执行规则

每天按下面顺序执行，计划表中的“产出”是当天的完成定义：

1. 先写 3～5 句学习笔记，说明今天解决了什么问题。
2. 再完成表格中的最小编码任务，不用追求一次写得很漂亮。
3. 至少补 1 个测试或 1 个可复现命令。
4. 提交一次 Git commit，格式示例：`week03-day02: add async http client`。
5. 遇到报错先记录“现象、猜测、验证、结论”，不要直接复制答案。

## 4. 12 周每日计划

### 路线与周次对应

| 阶段         | 周次          | 重点结果                                          |
| ------------ | ------------- | ------------------------------------------------- |
| Python 基础  | 第 1～3 周    | 能写模块化、类型安全、可测试的 Python 程序        |
| API 服务     | 第 4 周       | 能用 FastAPI 提供持久化、鉴权和可测试的 API       |
| LLM 调用     | 第 5 周       | 能调用模型、处理结构化输出、重试和成本            |
| Tool Calling | 第 6 周       | 能定义、注册、执行和保护模型工具                  |
| RAG          | 第 7 周       | 能导入资料、检索证据、生成引用并评测              |
| Agent 工作流 | 第 8 周前半段 | 能编排步骤、状态、暂停和失败恢复                  |
| MCP          | 第 8 周后半段 | 能开发 MCP Server，并让 Agent 发现和调用 MCP 工具 |
| 流式交互     | 第 9 周       | 能用 SSE 将 Agent/MCP 运行过程可靠呈现在前端      |
| 生产化       | 第 10～12 周  | 能测试、观测、加固、部署并交付毕业项目            |

### 第 1 周：环境、语法与可运行的 Python

**周目标：** 能读写基础 Python，理解对象、表达式、控制流，并做出一个命令行小工具。

| 天次  | 学习主题                  | 动手任务                                                                                                              | 当天产出                                                      |
| ----- | ------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| Day 1 | 环境与执行模型            | 安装 Python/uv；创建虚拟环境；分别运行 `.py`、`python -m` 和 REPL；配置 Ruff；认识 `pyproject.toml`。                 | `hello.py`、初始化仓库、环境说明和第一次 commit。             |
| Day 2 | 变量、类型与表达式        | 练习 `str`、`int`、`float`、`bool`、`None`；格式化字符串；类型转换；写一个“Token 成本估算器”（输入 token 数和单价）。 | `scripts/token_cost.py`，至少 5 个边界输入示例。              |
| Day 3 | 条件与循环                | 用 `if/elif`、`for`、`while`、`break/continue` 处理用户输入；实现猜数字和重试次数限制。                               | `scripts/guess.py`，能处理非法输入而不崩溃。                  |
| Day 4 | 函数与作用域              | 拆分函数；使用默认参数、关键字参数、返回值；为成本估算器加预算告警和纯函数测试。                                      | `app/budget.py`、`tests/test_budget.py`，至少 6 个断言。      |
| Day 5 | 小项目：CLI 对话记录器 v0 | 读取用户输入，保存为带时间的 JSON；支持 `add`、`list`、`quit`；画出输入到文件的流程。                                 | 可运行 CLI、`data/conversations.json` 示例、README 使用说明。 |

**周验收：** 删除 IDE 运行配置后，仍能用一条命令运行 CLI；对空输入、超长输入和不存在文件有明确行为。

### 第 2 周：数据结构、模块、文件与错误处理

**周目标：** 能把脚本拆成模块，可靠处理 JSON/文件，并开始用测试保护行为。

| 天次   | 学习主题               | 动手任务                                                                                      | 当天产出                                                           |
| ------ | ---------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Day 6  | list/dict/set/tuple    | 用列表和字典建模消息、用户和工具；练习推导式、排序和分组；把对话记录改成 `list[dict]`。       | `app/models.py` 的数据样例和 10 个结构操作练习。                   |
| Day 7  | 模块与包               | 设计 `cli.py`、`storage.py`、`models.py`；理解导入、`__name__` 和包边界；避免循环导入。       | CLI 重构为 3 个模块，入口命令仍可运行。                            |
| Day 8  | 文件、JSON 与 pathlib  | 用 `pathlib` 管理路径；实现原子写入、备份和 JSON schema 校验；处理 UTF-8。                    | `app/storage.py`，包含不存在文件、损坏 JSON、空文件的处理。        |
| Day 9  | 异常与日志             | 学习异常层级、`raise`、`try/except/else/finally`；用 `logging` 记录可定位信息；不要吞掉异常。 | 统一错误类型 `StorageError`，并有可读日志。                        |
| Day 10 | 测试与小项目：CLI Todo | 用 pytest 测试增删改查、持久化和异常；实现 `todo add/list/done/remove`，支持标签和优先级。    | `app/todo/`、`tests/`、覆盖核心路径的测试集和命令行演示 GIF/截图。 |

**周验收：** 核心业务逻辑与 CLI 解耦；测试可重复运行；`ruff check .` 通过。

### 第 3 周：面向对象、类型标注、迭代器与异步基础

**周目标：** 能读懂常见 Python 工程代码，知道何时使用类、协议和异步。

| 天次   | 学习主题                     | 动手任务                                                                                           | 当天产出                                                    |
| ------ | ---------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Day 11 | 类、dataclass 与封装         | 用 `dataclass` 定义 `Message`、`Conversation`、`ToolCall`；实现序列化和反序列化。                  | `app/domain.py`，对象可 round-trip 转为 JSON。              |
| Day 12 | 继承、组合与协议             | 比较继承和组合；定义 `Storage` Protocol；写内存存储和文件存储两个实现。                            | `app/ports.py`、两个存储适配器及替换示例。                  |
| Day 13 | 类型标注与泛型               | 使用 `Optional`/联合类型、`Literal`、`TypedDict`、泛型函数；配置 mypy 或 pyright。                 | mypy/pyright 检查通过，补齐第 2 周模块的类型。              |
| Day 14 | 迭代器、生成器与上下文管理器 | 用生成器逐条读取大文件；实现计时上下文管理器；理解惰性求值。                                       | `scripts/stream_jsonl.py`，处理 10 万行模拟数据且内存稳定。 |
| Day 15 | async/await 与 HTTP 入门     | 用 `asyncio` 并发执行模拟任务；比较同步/异步；用 `httpx.AsyncClient` 请求一个公开或本地 mock API。 | `app/http_client.py` 和基准记录，说明并发收益与限制。       |

**周验收：** 能解释协程、线程、进程的区别；所有公共函数都有参数和返回值类型；异步代码没有阻塞式 `requests` 调用。

### 第 4 周：Web API、FastAPI 与数据持久化

**周目标：** 能独立写出前端可消费的 Python API，为后续 Agent 服务打底。

| 天次   | 学习主题               | 动手任务                                                                                      | 当天产出                                                    |
| ------ | ---------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Day 16 | HTTP、REST 与 FastAPI  | 创建 FastAPI 应用；实现 health check、路由、状态码、请求/响应模型；用 Swagger 调试。          | `app/api/main.py`，`GET /health` 和 `GET /conversations`。  |
| Day 17 | Pydantic 与配置        | 定义输入输出模型、字段约束、嵌套模型；用 `pydantic-settings` 读取 `.env`；区分开发/生产配置。 | `app/settings.py`、统一的校验错误响应。                     |
| Day 18 | CRUD 与 SQLite         | 使用 `sqlite3` 或 SQLModel 完成会话/消息 CRUD；设计主键、时间字段和索引。                     | 数据库迁移说明、`/conversations` CRUD API。                 |
| Day 19 | 中间件、CORS、鉴权基础 | 配置 CORS、请求 ID、异常处理；实现最小 API Key/JWT 鉴权（只保护自己的 API，不存明文密码）。   | 前端可跨域调用的 API，未授权请求返回 401。                  |
| Day 20 | 小项目：Notes API      | 把前 3 周的记录器升级为 FastAPI Notes API；加入分页、搜索、标签、OpenAPI 文档和集成测试。     | 可运行 API、`tests/test_api.py`、curl 调用示例和 API 截图。 |

**周验收：** 新用户可通过 API 创建会话并读取消息；数据库重启后数据仍存在；`pytest` 和 `ruff` 均通过。

### 第 5 周：LLM API、提示词与结构化输出

**周目标：** 理解模型调用边界，能构建稳定的基础 Chat 服务。没有 API Key 时使用本地 fake provider 完成同样接口。

| 天次   | 学习主题                | 动手任务                                                                                                   | 当天产出                                                  |
| ------ | ----------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| Day 21 | 模型、消息与 token      | 理解 system/user/assistant/tool 消息、上下文窗口、temperature、token 和费用；比较 3 组提示词。             | `docs/llm-notes.md`，记录同一问题的提示词实验和成本估算。 |
| Day 22 | Provider 抽象与安全配置 | 封装 `LLMProvider` Protocol；用 `httpx` 调 OpenAI-compatible `/chat/completions`；Key 只从环境变量读取。   | `app/llm/provider.py`、真实 provider 和 fake provider。   |
| Day 23 | 结构化输出              | 用 Pydantic 定义 `Answer`、`Classification`；解析 JSON；处理模型输出缺字段、额外文本和重试。               | `/classify` API，非法模型输出有可测试的降级路径。         |
| Day 24 | 调用可靠性与错误处理    | 为模型请求加入连接/读取超时、指数退避、最大重试次数和 provider 错误映射；用 fake provider 模拟限流和超时。 | `app/llm/retry.py`、失败场景测试和错误码说明。            |
| Day 25 | 小项目：可靠 Chat API   | 合并 provider、结构化输出、重试、超时、请求日志和 token 用量记录；先完成稳定的非流式 `/chat` 接口。        | `app/chat/`、单元/集成测试、失败重试演示、API 文档。      |

**周验收：** 模型不可用时 API 返回可理解的错误；密钥不出现在日志；超时和重试行为可复现，非流式 `/chat` 请求不会遗留后台任务。

### 第 6 周：Tool Calling：Schema、执行循环与安全

**周目标：** 理解模型提出工具调用和应用实际执行工具之间的边界，完成可控、可测试的 Tool Calling 闭环。

| 天次   | 学习主题                  | 动手任务                                                                                                            | 当天产出                                                |
| ------ | ------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| Day 26 | Tool Calling 消息协议     | 学习 tool schema、tool call id、arguments、tool result 和消息往返；手工画出一次模型请求到工具结果的完整序列。       | `docs/tool-calling.md`、一份可回放的 tool-call JSON。   |
| Day 27 | Schema 与工具注册         | 用 Pydantic 生成 JSON Schema；定义 `Tool` Protocol、名称、描述、参数和注册表；实现 calculator、date、search_notes。 | `app/agent/tools/`，参数校验失败有结构化错误。          |
| Day 28 | 执行器与 Agent loop       | 编写 executor：解析 arguments、分发工具、设置超时、重试和最大步数；检测重复 call 和无效 tool id。                   | `app/agent/executor.py`、成功/失败/超时测试和执行轨迹。 |
| Day 29 | 权限、审批与幂等          | 区分只读/有副作用工具；加入 allowlist、人工确认、幂等键、输入上限和审计；拒绝任意 shell 执行。                      | 工具策略文档、审批流程测试、注入攻击样例和审计事件。    |
| Day 30 | 小项目：个人效率 Agent v1 | 组合日历 mock、Todo、笔记搜索和计算器；支持查询与需要确认的写操作；补 10 条 Tool Calling 行为测试。                 | 可录屏 CLI Agent、工具调用轨迹 JSON、架构图和失败案例。 |

**周验收：** 能解释 tool call 与 tool result 的消息关系；Agent 最多执行配置的步数；每次工具调用都有输入、输出、耗时和结果状态；副作用操作必须确认。

### 第 7 周：RAG：数据导入、检索、引用与评测

**周目标：** 能做出“回答有依据”的知识库 Agent，而不是只依赖模型记忆。

| 天次   | 学习主题               | 动手任务                                                                                               | 当天产出                                                |
| ------ | ---------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| Day 31 | RAG 全链路与数据建模   | 画出 ingestion、embedding、index、retrieve、generate 流程；定义 `Document`、`Chunk`、`Citation`。      | `docs/rag-design.md` 和 Pydantic 数据模型。             |
| Day 32 | 文档清洗与切分         | 支持 Markdown/纯文本；去重、保留标题层级、按 token/字符切块；记录 source、位置和版本。                 | `scripts/ingest.py`，用 20 篇示例文档生成 JSONL。       |
| Day 33 | Embedding 与向量索引   | 接入 embedding provider 或本地 mock；先用 NumPy/余弦相似度做基线，再接 FAISS/Chroma（二选一）。        | `app/rag/index.py`，可重建索引并持久化。                |
| Day 34 | 检索、引用与拒答       | 实现 top-k、相似度阈值、上下文拼接、引用编号；资料不足时明确拒答，不编造答案。                         | `/knowledge/query` API，返回 answer、citations、score。 |
| Day 35 | 小项目：文档问答 Agent | 导入项目 README/产品文档；结合第 6 周工具；准备 20 条问题和标准答案，测 Recall@k、引用命中率和拒答率。 | 可演示 RAG Agent、评测 CSV、失败案例分析和改进清单。    |

**周验收：** 任意回答都能追溯到文档片段；更新文档后可增量重建；评测集不与提示词硬编码在一起。

### 第 8 周：Agent 工作流与 MCP

**周目标：** 先用工作流管理复杂任务，再掌握 MCP 的 Host/Client/Server 边界，让外部工具以标准协议接入 Agent。

| 天次   | 学习主题                      | 动手任务                                                                                                                             | 当天产出                                                  |
| ------ | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| Day 36 | 工作流建模                    | 把“研究 -> 检索 -> 草稿 -> 审核 -> 输出”画成有向图；定义节点输入输出和失败转移。                                                     | `docs/workflow.md`、一份状态转移表。                      |
| Day 37 | 工作流执行与状态恢复          | 先用纯 Python 实现 workflow runner；加入 state、checkpoint、暂停/恢复和失败转移；再选 LangGraph 复刻关键流程。                       | `app/workflow/`、状态快照、框架版与原生版对比记录。       |
| Day 38 | MCP 核心概念                  | 理解 MCP Host、Client、Server、JSON-RPC 生命周期与 capabilities；区分 tools、resources、prompts 与普通函数调用。                     | `docs/mcp-notes.md`、MCP 与 Tool Calling 的边界对比表。   |
| Day 39 | MCP Server 与 Client          | 优先使用官方 Python MCP SDK，实现 calculator/search_notes MCP Server；用 stdio 或 HTTP 连接 Client，完成发现、调用、错误和权限校验。 | 可独立启动的 MCP Server、Client 连接脚本、协议抓包/日志。 |
| Day 40 | 小项目：工作流 + MCP 研究助手 | 将研究工作流接入 MCP 工具；Agent 先发现可用工具，再调用检索/计算；记录 workflow、tool 和 MCP 事件，并支持失败回退。                  | 端到端 demo、MCP 配置说明、3 个恢复测试和调用轨迹。       |

**周验收：** 能画出工作流状态转移；能独立启动 MCP Server 并被 Client 发现；MCP 工具调用有权限边界、超时和错误回退；一次运行可查询完整轨迹。

### 第 9 周：MCP 接入后的流式交互与 Agent 前端

**周目标：** 将 Agent、Tool Calling 和 MCP 的运行过程通过 SSE 可靠地呈现在前端，并处理真实网络下的中断和恢复。

| 天次   | 学习主题              | 动手任务                                                                                                                                | 当天产出                                                |
| ------ | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| Day 41 | 事件协议与 SSE 基础   | 定义 `run.started`、`step.started`、`tool.called`、`mcp.call.started`、`token.delta`、`run.completed`、`run.failed`；实现可回放 JSONL。 | `app/events/schema.py`、事件版本说明和协议测试。        |
| Day 42 | 后端流式交互          | 把模型 token、Tool Calling、MCP 调用和工作流状态转成 SSE；加入心跳、request_id、取消、超时和断线后的 run 查询。                         | `/runs/{run_id}/events` 端点、curl 流式演示和取消测试。 |
| Day 43 | 流式聊天 UI           | 用 Fetch Streams 或 EventSource 解析事件；实现消息列表、token 增量合并、发送/停止按钮、自动滚动和错误恢复。                             | 可连接第 8 周 Agent/MCP 服务的 Chat 页面。              |
| Day 44 | 工具过程与引用展示    | 展示 Tool Calling 参数摘要、MCP Server/工具名称、步骤状态、耗时和 RAG 引用；敏感参数折叠；处理重复事件。                                | Tool/MCP timeline、Citation panel、空态和加载态。       |
| Day 45 | 小项目：Agent Chat v1 | 将研究助手接入真实页面；加入会话切换、重新生成、导出 Markdown、反馈、停止和断线恢复。                                                   | 可部署前端、3 分钟演示视频/截图、前后端联调说明。       |

**周验收：** 用户能看懂模型、Tool Calling、MCP 和 RAG 正在做什么；流式请求可停止和恢复；模型慢、工具失败、MCP 断开、空结果时界面仍稳定；不把 API Key 放进浏览器。

### 第 10 周：质量、安全、观测与部署

**周目标：** 把 demo 变成可维护的服务，知道 Agent 产品最常见的风险在哪里。

| 天次   | 学习主题          | 动手任务                                                                                                  | 当天产出                                             |
| ------ | ----------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| Day 46 | 测试金字塔        | 为纯函数写单测，为 API 写集成测，为 Agent 用 fake model 做行为测；固定随机性和时间。                      | 至少 30 个测试，列出尚未覆盖的风险。                 |
| Day 47 | 评测与回归        | 建立 30 条 golden set；定义任务成功率、引用正确率、工具选择准确率、延迟和成本；每次提示词改动自动跑评测。 | `evals/` 脚本、基线 JSON、回归报告模板。             |
| Day 48 | 安全与滥用防护    | 练习 prompt injection、越权工具调用、敏感信息泄露、SSRF 和超长输入；加入输入过滤、权限校验、限流和审计。  | Threat model、红队样例 15 条、修复前后结果。         |
| Day 49 | 日志、追踪与成本  | 结构化记录 request_id/run_id、模型、token、耗时、工具结果；脱敏用户内容；加入健康检查和基础 metrics。     | 可检索日志样例、成本看板字段说明、故障排查 Runbook。 |
| Day 50 | Docker、CI 与发布 | 编写 Dockerfile/compose；用 GitHub Actions（或本地等价 CI）跑 lint、typecheck、test；准备生产环境变量。   | 一条命令启动全栈；CI 通过；发布/回滚说明。           |

**周验收：** 能用一条命令从干净环境启动；测试失败会阻止发布；日志里找得到一次完整 run，但看不到密钥和原始敏感数据。

### 第 11 周：毕业项目冲刺一：设计、后端与知识库

**周目标：** 正式启动项目级实战，把前 10 周能力组合成一个完整产品。

#### 毕业项目：Agent 工作台（Agent Workbench）

为团队文档和任务处理提供一个可追溯的 Agent 工作台。用户可以上传/导入知识文档，向 Agent 提问或发起任务；Agent 使用 Tool Calling 调度检索、计算、任务管理和 MCP 工具，并实时展示步骤、引用和需要人工确认的动作。

建议技术栈：

- 前端：React + TypeScript + 现有熟悉的 UI 方案。
- 后端：Python + FastAPI + Pydantic + SQLite（生产可替换 PostgreSQL）。
- Agent：自建最小循环或 LangGraph；Provider 做成可替换接口；Tool Calling 负责工具调度。
- MCP：实现至少一个 Python MCP Server，并由 Agent Client 发现和调用；记录工具权限与调用轨迹。
- 检索：FAISS/Chroma 之一；文档先支持 Markdown/HTML，PDF 作为加分项。
- 交互：SSE 流式事件；Docker Compose 本地启动。

| 天次   | 学习主题                          | 动手任务                                                                                                                        | 当天产出                                                             |
| ------ | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Day 51 | 需求与验收                        | 写用户故事、非目标、风险和 5 条核心流程；定义“成功”而不是只列功能。                                                             | `docs/product-spec.md`，含验收条件和演示脚本。                       |
| Day 52 | 架构与数据模型                    | 画前端、API、Agent、RAG、数据库和模型 Provider 的边界；设计 User、Conversation、Message、Run、Document、Citation、Approval 表。 | `docs/architecture.md`、ER 图、ADR-001。                             |
| Day 53 | 后端骨架与鉴权                    | 建 FastAPI app、配置、数据库迁移、统一错误、request_id、最小用户/API Key 鉴权。                                                 | 可启动后端、健康检查、迁移命令和 API contract。                      |
| Day 54 | 文档导入与 RAG                    | 实现上传/导入、解析、切分、索引、删除和重建；保存 source/version；为每个 chunk 生成可点击引用。                                 | `/documents` 全流程、示例知识库、导入失败重试。                      |
| Day 55 | Agent、Tool Calling 与 MCP 工具层 | 接入 provider；实现知识检索、计算、任务 CRUD 三个工具，并将其中至少一个封装为 MCP Server；加入步数、超时、权限和审批策略。      | `run` API、Tool Calling/MCP 测试、一次包含 MCP 调用的完整 run 轨迹。 |

**第 11 周里程碑：** 只用 curl/脚本即可完成“创建会话 -> 导入文档 -> 发起任务 -> 获取引用和工具结果”。

### 第 12 周：毕业项目冲刺二：前端、质量与发布

**周目标：** 完成可展示、可复现、可维护的项目，并形成作品集材料。

| 天次   | 学习主题             | 动手任务                                                                                                      | 当天产出                                                    |
| ------ | -------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Day 56 | 工作台前端骨架       | 完成会话侧栏、消息区、输入区、知识库页、运行详情页；先用 mock 事件打通布局。                                  | 响应式页面骨架、loading/empty/error 状态。                  |
| Day 57 | 真实流式联调         | 接入 SSE；显示 token、工作流步骤、Tool Calling、MCP Server/工具、引用和审批；实现停止、重试、刷新后恢复 run。 | 前后端端到端流程，录制一次无剪辑演示。                      |
| Day 58 | 评测、安全与性能收口 | 跑 golden set 和红队样例；修复最高优先级问题；检查 N+1、超长上下文、缓存和首 token 延迟。                     | `docs/evaluation-report.md`、风险清单关闭记录、性能基线。   |
| Day 59 | 部署与可观测性       | 用 Docker Compose 部署；配置反向代理/HTTPS（可用托管平台）；验证冷启动、重启、日志、备份和回滚。              | 可分享的演示地址或本地复现包、部署 Runbook。                |
| Day 60 | 发布、复盘与作品集   | 写项目 README、架构图、技术取舍、已知限制和下一步；打 tag；准备 5 分钟技术讲解和简历项目描述。                | 完整 Git 仓库、演示视频/截图、评测数据、`v1.0.0` 发布说明。 |

**第 12 周里程碑：** 新机器按 README 在 30 分钟内启动；演示脚本能完整跑通；评测、日志、错误处理和安全限制都有证据。

## 5. 毕业项目验收清单

提交项目之前逐项打勾：

- [ ] 前端没有暴露模型 API Key，所有模型请求经过后端。
- [ ] 支持新建/切换/删除会话，刷新页面后历史仍可读取。
- [ ] 支持 SSE 流式 token，用户可停止运行，断线后有明确提示。
- [ ] Tool Calling 至少有 3 个工具，具备 schema 校验、最大步数、超时、重试和错误回退。
- [ ] 至少有 1 个 MCP Server，可被 Agent Client 发现和调用，并有工具权限边界。
- [ ] 至少 1 个 Tool Calling/MCP 工具有副作用且必须人工确认。
- [ ] Agent 有最大步数、单步超时、总超时和错误恢复策略。
- [ ] RAG 回答带引用；资料不足时会拒答；文档可重建索引。
- [ ] 每次 run 有 run_id、事件轨迹、耗时和 token/成本记录。
- [ ] 有单元测试、API 集成测试、Agent 行为测试和一套固定评测集。
- [ ] 已测试 prompt injection、越权工具、敏感信息和超长输入。
- [ ] `ruff check .`、类型检查、测试和前端构建均通过。
- [ ] Docker 可启动；`.env.example` 完整；README 有安装、配置、演示和故障排查。

## 6. 推荐的每周复盘模板

每周五复制下面模板到 `docs/weekly/`，用事实记录进度：

```markdown
# Week N 复盘

## 本周交付

- （填写）

## 我能解释的 3 个概念

1.
2.
3.

## 本周最难的 bug

- 现象：
- 猜测：
- 验证：
- 结论：

## 测试与指标

- 测试数量：
- 通过率：
- 延迟/成本/Recall 等指标：

## 下周唯一优先事项

-
```

## 7. 学完后的进阶顺序

完成毕业项目后，按实际工作目标选择一条深入：

1. **Agent 平台工程：** PostgreSQL、Redis、队列、租户隔离、配额、审计和灰度发布。
2. **检索与知识工程：** 混合检索、reranker、知识图谱、文档解析质量和离线评测。
3. **模型与推理：** Transformers、量化、vLLM、微调和模型路由。
4. **Agent UX：** 任务可视化、可编辑计划、人工接管、协作和可访问性。
5. **可靠性与安全：** OpenTelemetry、红队评测、策略引擎、隐私计算和合规。

不要同时开启五条线。先让毕业项目稳定运行，再根据真实用户反馈选择一个方向做 4 周深挖。

## 8. 最终成果应包含什么

12 周结束时，你的作品集至少应有：

- 一个公开 Git 仓库和清晰的项目 README。
- 一张架构图、一份 API/事件协议文档和一份安全/评测报告。
- 一个可运行的 Agent 工作台，而非只有 notebook 或截图。
- 一段 3～5 分钟演示：提问、检索引用、工具调用、人工确认、失败恢复。
- 一组可量化结果：测试数量、评测集表现、首 token 延迟、单次成本或资源占用。
- 一份复盘：哪些设计被验证、哪些没有解决、下一版准备怎么改。

这套计划按每周 5 天计算，共 60 天。周末可以休息；若想加练，只做复盘、阅读源码或改善毕业项目，不把新知识堆到主线里。
